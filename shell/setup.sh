#!/bin/bash

brew install zsh
brew install starship
echo 'eval "$(starship init zsh)"' >> ~/.zshrc
brew install tmux
brew install tree
brew install htop
brew install wget
brew install zoxide
brew install btop
brew install tmuxinator
brew install go-task